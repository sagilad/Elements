<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.flow8.elements">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/launch_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".WoodActivity" />
        <activity android:name=".FireActivity" />
        <activity android:name=".EarthActivity" />
        <activity android:name=".MetalActivity" />
        <activity android:name=".WaterActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NameBirth" />
        <activity android:name=".preQuestionsActivity" />
        <activity android:name=".resultActivity"></activity>
    </application>

</manifest>